 <div>
   <p id="notice"><%= notice %></p>
</div>

<div>
    <div id="titre">
    <h1>Emails</h1>
    </div>
    <div id="dede">
        <section id="left">
              <table>
                <thead>
                  <tr>
                    <th>Object</th>
                    <th>Statu</th>
                    <th>Reçu le</th>
                    <th colspan="3"></th>
                  </tr>
                </thead>

                <tbody>
                  <% @emails.each do |email| %>
                    <tr>
                      <td><%= email.object %></td>
                      <td><%= email.read %></td>
                      <td><%= email.created_at.strftime("%m/%d/%Y") %></td>
                      <td><%= link_to 'Voir', email, remote: true  %></td>        
                    </tr>
                  <% end %>
                </tbody>
                
              </table>

              <br>
              
        </section>

        <section id="right">
                <div id="email-body" style="display: none;"></div>
        </section>
    </div>
    <div id="nouv">
        <%= link_to 'New Email', new_email_path %>
    </div>
</div>